import React, { Component } from "react";
import { SectionTitle } from "./";

export default class Services extends Component {
  render() {
    return (
        <div>
            <SectionTitle title="A tavola non si invecchia" subtitle="Located in Staten Island, New York City" />
        </div>
    )
  }
}
